# Database files — exclude temp/test DBs, but track the main one
*.db
*.db-journal
*.db-wal
*.db-shm
test_write.db*
!skahl_historical.db

# Linux FUSE artifacts
.fuse_hidden*

# IDE / editor config
.idea/
*.pyc
__pycache__/

# Claude session files
.claude/

# Scraped raw data — exclude intermediate/debug files, track the ones the pipeline needs
current_season_data.json
additional_seasons_data.json
detailed_season_analysis.json

# Raw network captures (large, not needed in repo)
*.har
